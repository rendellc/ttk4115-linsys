toppath = genpath(pwd);
addpath(toppath);

pushd('../');
pushd('data - complete/')



files = dir();
for i=3:(size(files,1) - 1) 
    if files(i).isdir == 0 && strcmp(files(i).name((end-2):end), "mat") 
        files(i).name
    end
end

rmpath(toppath);
